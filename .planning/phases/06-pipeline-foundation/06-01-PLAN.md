---
phase: 06-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-svgs.js
  - scripts/build-template.js
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "User can create visual assets named '-dark-only.svg' without build errors."
    - "Frieren dark-theme colors are enforced for '-dark-only.svg' files."
    - "User can write a '<picture>' with just an '<img>' or '<video>' tag and it passes through build-template.js unwrapped."
  artifacts:
    - path: "scripts/verify-svgs.js"
      provides: "SVG Validation logic allowing dark-only variants"
    - path: "scripts/build-template.js"
      provides: "Template processing logic unwrapping dark-only assets"
  key_links:
    - from: "scripts/verify-svgs.js"
      to: "assets/images"
      via: "fs.readdirSync"
      pattern: "\\-dark\\-only\\.svg"
    - from: "scripts/build-template.js"
      to: "README.template.md"
      via: "Regex Replacement"
      pattern: "<(img|video)[^>]*>"
---

<objective>
Update build scripts to support dark-theme-only visual assets, ensuring CI validation passes without requiring light-theme pairs and allowing these assets to render correctly in the final README.

Purpose: To prepare the build pipeline for dynamic algorithmic art and thematic SVGs which are optimized exclusively for dark mode.
Output: Updated `verify-svgs.js` and `build-template.js` scripts.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-foundation/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update verify-svgs.js to support dark-only assets</name>
  <files>scripts/verify-svgs.js</files>
  <behavior>
    - Test 1: SVGs ending in `-dark-only.svg` must pass if they contain dark theme colors.
    - Test 2: SVGs ending in `-dark-only.svg` must NOT trigger the "Missing light mode variant" assertion.
  </behavior>
  <action>
    Update `scripts/verify-svgs.js`:
    1. In the color theme check, handle `file.endsWith('-dark-only.svg')` identical to `-dark.svg` (must enforce dark theme colors).
    2. In the matching variants check, filter out `-dark-only.svg` files so they do not get added to `darkFiles` which requires a corresponding `-light.svg`.
    3. During execution, test the behavior by temporarily creating a mock `test-dark-only.svg` in `assets/images/` and asserting it passes, then delete it.
  </action>
  <verify>
    <automated>node scripts/verify-svgs.js</automated>
  </verify>
  <done>verify-svgs.js accepts `-dark-only.svg` files without demanding a light pair, while still enforcing dark colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update build-template.js to unwrap single-element picture tags</name>
  <files>scripts/build-template.js</files>
  <behavior>
    - Test 1: `<picture>` tags with `<source>` and `<img>` are replaced with dual GitHub mode suffixes.
    - Test 2: `<picture>` tags containing ONLY an `<img>` (no `<source>`) are unwrapped, outputting the exact `<img>` tag without suffixes.
    - Test 3: `<picture>` tags containing ONLY a `<video>` tag (no `<source>`) are unwrapped, outputting the exact `<video>` tag without suffixes.
  </behavior>
  <action>
    Update `scripts/build-template.js`:
    1. In the `content.replace` callback, if `!sourceMatch` is true, check for an `<img>` or `<video>` match within `inner` using `/<(img|video)[^>]*>/i`.
    2. If found, return the full matched inner tag (e.g., `innerMatch[0]`) instead of `match` (untouched) to unwrap it.
    3. If neither is found, return `match`.
    4. During execution, test the behavior by temporarily creating a mock template structure, processing it, and asserting correct output.
  </action>
  <verify>
    <automated>node scripts/build-template.js</automated>
  </verify>
  <done>build-template.js correctly unwraps `<picture>` tags lacking `<source>`, preserving the inner `<img>` or `<video>`.</done>
</task>

</tasks>

<verification>
Ensure both scripts complete successfully via `npm run build` or their respective `node` invocations.
</verification>

<success_criteria>
- `verify-svgs.js` correctly filters out `-dark-only.svg` from pairing requirements but enforces colors.
- `build-template.js` successfully unwraps single-element picture tags containing `<img>` or `<video>`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-foundation/06-01-SUMMARY.md`
</output>
