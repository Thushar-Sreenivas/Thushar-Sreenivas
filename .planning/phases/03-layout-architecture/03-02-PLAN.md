---
phase: 03-layout-architecture
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .github/workflows/build.yml
autonomous: false
requirements:
  - LAYT-01
  - LAYT-02
  - LAYT-03
must_haves:
  truths:
    - Pushing changes to the repository automatically builds and commits the README.md
    - The final README layout works correctly on both desktop and mobile
    - Light and dark themes display the correct SVG assets
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "CI/CD automated build step"
  key_links:
    - from: ".github/workflows/build.yml"
      to: "package.json"
      via: "npm run build"
      pattern: "run: npm run build"
---

<objective>
Automate the build process using GitHub Actions and perform a final human verification of the profile's visual integrity across different themes and devices.

Purpose: Ensures the repository remains a self-updating system when the template is edited, and confirms mobile/dark-mode requirements are fully satisfied.
Output: A GitHub Actions workflow file and verified cross-platform visuals.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-architecture/03-CONTEXT.md
@.planning/phases/03-layout-architecture/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup GitHub Action for auto-commit</name>
  <files>.github/workflows/build.yml</files>
  <action>
    Create a GitHub workflow that triggers on push to the `main` or `master` branch. The job should checkout the repository, setup Node.js, run `npm run build`, and use `stefanzweifel/git-auto-commit-action@v5` to commit any changes to `README.md` and `assets/` back to the repository using the default `GITHUB_TOKEN`.
  </action>
  <verify>
    <automated>test -f .github/workflows/build.yml && grep "stefanzweifel/git-auto-commit-action" .github/workflows/build.yml</automated>
  </verify>
  <done>GitHub Action is configured to build and commit changes automatically</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and CI Verification</name>
  <action>Pause for human to verify the layout and visuals.</action>
  <verify>Human visual confirmation</verify>
  <done>Human approves the visuals and CI workflow setup</done>
  <what-built>The fully automated responsive profile pipeline</what-built>
  <how-to-verify>
    1. Run `npm run build` locally to ensure no errors occur.
    2. Open the generated `README.md` in a GitHub-compatible markdown preview.
    3. Ensure no horizontal scrollbars exist on a simulated mobile viewport.
    4. Verify that `#gh-light-mode-only` and `#gh-dark-mode-only` tags are correctly placed and render appropriately when themes are toggled (if possible, or verify code manually).
  </how-to-verify>
  <resume-signal>Type "approved" if visuals and CI setup look correct</resume-signal>
</task>

</tasks>

<verification>
Automated check that the workflow YAML is valid and human confirmation of visual success.
</verification>

<success_criteria>
The layout architecture is fully implemented, verified, and ready for continuous integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-architecture/03-02-SUMMARY.md`
</output>