---
phase: 03-layout-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-assets.js
  - scripts/build-template.js
  - package.json
autonomous: true
requirements:
  - LAYT-01
  - LAYT-02
  - LAYT-03
must_haves:
  truths:
    - Developer can run `npm run build` to generate the final README
    - Output README contains Markdown images with `#gh-light-mode-only` and `#gh-dark-mode-only` URL fragments
    - Generated SVGs scale fluidly without horizontal scroll on mobile devices
  artifacts:
    - path: "scripts/build-template.js"
      provides: "Template conversion script"
    - path: "scripts/generate-assets.js"
      provides: "Responsive SVGs using viewBox"
    - path: "package.json"
      provides: "Build command orchestration"
  key_links:
    - from: "package.json"
      to: "scripts/"
      via: "npm run build command"
      pattern: "node scripts/generate-assets.js && node scripts/build-template.js && node scripts/verify-content.js"
---

<objective>
Implement the template-driven build architecture to generate the final `README.md` with responsive SVGs and proper GitHub Markdown theming.

Purpose: Creates a reliable, zero-dependency build pipeline that converts HTML picture tags into platform-optimized Markdown.
Output: Responsive SVGs and a robust `build-template.js` pipeline triggered via `npm run build`.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-layout-architecture/03-CONTEXT.md
@.planning/phases/03-layout-architecture/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SVG generator for responsiveness</name>
  <files>scripts/generate-assets.js</files>
  <action>
    Modify the SVG generation functions to output responsive SVGs. Replace hardcoded `width` and `height` attributes on the root `<svg>` tags with intrinsic `viewBox` attributes (e.g., `viewBox="0 0 800 200"`) and add `width="100%"`. Do not alter the theme colors or internal elements.
  </action>
  <verify>
    <automated>node scripts/generate-assets.js && grep "viewBox" assets/images/hero-banner-light.svg</automated>
  </verify>
  <done>Generated SVGs use viewBox instead of fixed width/height for natural mobile scaling</done>
</task>

<task type="auto">
  <name>Task 2: Create template builder script</name>
  <files>scripts/build-template.js</files>
  <action>
    Create a Node.js script that reads `README.template.md`. Use a robust regular expression to parse HTML `<picture>` blocks. Extract the dark `srcset` path, light `src` path, and `alt` text. Replace the entire `<picture>` block with pure Markdown format: `![AltText](lightSrc#gh-light-mode-only)\n![AltText](darkSrc#gh-dark-mode-only)`. Write the output to `README.md`. Handle absolute paths safely relative to the script's directory.
  </action>
  <verify>
    <automated>node scripts/build-template.js && grep "gh-dark-mode-only" README.md</automated>
  </verify>
  <done>README.md generated from template with HTML tags completely stripped and replaced by Markdown image fragments</done>
</task>

<task type="auto">
  <name>Task 3: Setup NPM build orchestration</name>
  <files>package.json</files>
  <action>
    Update `package.json` to include a `build` script in the `scripts` object. The script should sequentially run asset generation, template building, and content verification. Example: `"build": "node scripts/generate-assets.js && node scripts/build-template.js && node scripts/verify-content.js"`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>package.json exposes a single `build` command that successfully processes and verifies the profile</done>
</task>

</tasks>

<verification>
Ensure `npm run build` completely regenerates the `assets/images/` directory and creates `README.md` correctly matching the layout requirements.
</verification>

<success_criteria>
`README.md` exists, contains no HTML `<picture>` tags, uses `#gh-dark-mode-only` markers, and passes the `verify-content.js` script.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-architecture/03-01-SUMMARY.md`
</output>