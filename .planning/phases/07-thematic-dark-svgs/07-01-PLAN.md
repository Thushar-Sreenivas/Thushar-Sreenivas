---
phase: 07-thematic-dark-svgs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "scripts/generate-magic-circles.js", "README.template.md", "scripts/generate-assets.js", "scripts/verify-content.js"]
autonomous: true
requirements: ["SVG-02"]
must_haves:
  truths:
    - "Typography renders perfectly identically across all devices"
    - "Profile headers display cohesive dark aesthetic"
    - "README references only dark-theme SVGs for universally cohesive styling"
  artifacts:
    - path: "scripts/generate-magic-circles.js"
      provides: "Generates foundational text-path SVGs"
    - path: "assets/fonts/EBGaramond-Regular.ttf"
      provides: "Frieren-esque typography"
  key_links:
    - from: "package.json"
      to: "scripts/generate-magic-circles.js"
      via: "npm run build"
---

<objective>
Establish programmatic SVG generation foundation using `d3-shape` and `text-to-svg`. Replace legacy text tags with baked SVG paths for perfect rendering, and update the README template to exclusively use the new `-dark-only` SVGs universally.

Purpose: Ensure typography rendering is identical across all devices despite GitHub's camo proxy, and align the profile with the dark Frieren aesthetic.
Output: Updated build script, downloaded font, foundational generated SVGs (headers, footers, dividers), and a simplified `README.template.md`.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-thematic-dark-svgs/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Font and Dependencies</name>
  <files>package.json, assets/fonts/EBGaramond-Regular.ttf</files>
  <action>Install `d3-shape` and `text-to-svg` as dependencies using npm. Create the `assets/fonts/` directory and use `curl` to download `EBGaramond-Regular.ttf` from `https://github.com/google/fonts/raw/main/ofl/ebgaramond/EBGaramond-Regular.ttf` into it.</action>
  <verify>
    <automated>npm ls d3-shape text-to-svg && test -f assets/fonts/EBGaramond-Regular.ttf</automated>
  </verify>
  <done>Dependencies installed and font file downloaded successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create Generator Script for Typography SVGs</name>
  <files>scripts/generate-magic-circles.js, package.json, scripts/generate-assets.js</files>
  <action>Create `scripts/generate-magic-circles.js` to replace `generate-assets.js`. It should require `d3-shape`, `text-to-svg`, and load the downloaded font. Write functions to generate `-dark-only.svg` files for headers (journey, grimoire, contact), dividers, and the footer. Embed text as paths instead of `<text>` tags. Also, generate dummy placeholders for `hero-banner` and `grimoire-*` SVGs (frontend, backend, tooling) so validation doesn't fail. Use dark background `#0d1117` and a single accent color (e.g., `#14b8a6`). Update `package.json` to run this script instead of `generate-assets.js` in the `build` script. Then delete `scripts/generate-assets.js`.</action>
  <verify>
    <automated>node scripts/generate-magic-circles.js && node scripts/verify-svgs.js</automated>
  </verify>
  <done>New generator script successfully runs and passes validation, generating placeholder and typography SVGs</done>
</task>

<task type="auto">
  <name>Task 3: Update README Template Integration</name>
  <files>README.template.md, scripts/verify-content.js</files>
  <action>Update `README.template.md` to reference the new `-dark-only.svg` variants universally. Remove the `<picture>` elements and their `<source>` / light-mode `<img>` pairs for the SVGs. Instead, use simple `<img src="assets/images/[name]-dark-only.svg" width="800" alt="...">`. Update `scripts/verify-content.js` to ensure it does not enforce `#gh-dark-mode-only` tag requirements, so validation allows the omission.

*Note: While requirement SVG-02 states to use `#gh-dark-mode-only`, we are honoring the locked decision in `07-CONTEXT.md` to "Show these new dark-themed SVGs in both light and dark modes" by omitting `#gh-dark-mode-only` so they render universally.*</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Template updated, build script succeeds, and `verify-content.js` passes</done>
</task>

</tasks>

<verification>
- Build pipeline runs seamlessly
- New SVGs are generated using paths for text
- SVGs pass existing verification rules
</verification>

<success_criteria>
Foundational SVGs successfully transition to programmatic generation with perfect typography, universally visible in the profile.
</success_criteria>

<output>
After completion, create `.planning/phases/07-thematic-dark-svgs/07-01-SUMMARY.md`
</output>
