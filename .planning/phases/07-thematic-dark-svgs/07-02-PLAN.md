---
phase: 07-thematic-dark-svgs
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["scripts/generate-magic-circles.js"]
autonomous: true
requirements: ["SVG-01"]
must_haves:
  truths:
    - "Hero banner features a highly intricate magic circle with orbiting elements"
    - "Grimoire SVGs feature distinct geometric patterns for frontend, backend, and tooling"
    - "SVGs include subtle animations via <animateTransform>"
  artifacts:
    - path: "scripts/generate-magic-circles.js"
      provides: "Generates complex d3-shape mathematical circles"
  key_links:
    - from: "scripts/generate-magic-circles.js"
      to: "assets/images/"
      via: "Outputs -dark-only.svg Frieren-themed visuals"
---

<objective>
Implement visually stunning, highly intricate Frieren-themed magic circles using `d3-shape` in Node.js. Replace placeholders with complex animated hero and grimoire SVGs.

Purpose: Deliver the core visual promise of the "Frieren" theme—complex, mathematical, glowing magic circles representing technical depth.
Output: Final implementation of `generate-magic-circles.js` producing high-quality `-dark-only.svg` variants for the hero and grimoires.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-thematic-dark-svgs/07-CONTEXT.md
</context>

<interfaces>
<!-- Key elements for the magic circles -->
From 07-CONTEXT.md & 07-RESEARCH.md:
- Colors: Dark Background `#0d1117`, Accent (e.g., `#14b8a6` teal or `#581c87` purple)
- Libraries: `d3-shape` (for complex paths like `d3.arc()`) and `text-to-svg` (for embedded text)
- Animations: Use native `<animateTransform>` for rotation (avoid CSS `<style>`)
- Sizing: Use flexible `viewBox` but scale text disproportionately larger so it's readable on mobile.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Hero Magic Circle</name>
  <files>scripts/generate-magic-circles.js</files>
  <action>Update `generate-magic-circles.js` to replace the placeholder `hero-banner` generator. Implement a large, highly intricate central magic circle using `d3-shape` (arcs, nested rings, geometric stars). Include smaller orbiting or connected circles. Apply subtle rotation animations using `<animateTransform>` on specific rings. Ensure the "There's something about making code work..." quote is embedded via `text-to-svg` paths and positioned legibly. Use unique IDs for any gradients/filters to prevent collisions (e.g., `id="glow-hero"`).</action>
  <verify>
    <automated>node scripts/generate-magic-circles.js && node scripts/verify-svgs.js</automated>
  </verify>
  <done>Hero banner SVG is generated with complex geometry, embedded text paths, and animation</done>
</task>

<task type="auto">
  <name>Task 2: Implement Grimoire Pattern Magic Circles</name>
  <files>scripts/generate-magic-circles.js</files>
  <action>Update `generate-magic-circles.js` to replace the placeholder `grimoire-*` generators (frontend, backend, tooling). Implement distinct geometric patterns for each section (e.g., overlapping triangles for frontend, concentric pentagrams for backend, interlocking gears/hexagons for tooling). Use `text-to-svg` for headers and tech stack labels. Apply subtle animations (like slow pulsing or counter-rotating rings). Ensure generated files are `grimoire-[name]-dark-only.svg`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All three grimoire SVGs are generated with distinct patterns and pass validation</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Output files (`hero-banner-dark-only.svg`, `grimoire-frontend-dark-only.svg`, etc.) contain `<animateTransform>` tags
- Output files contain complex `<path>` elements replacing standard `<text>` tags
</verification>

<success_criteria>
Beautiful, complex, animated Frieren-themed SVG magic circles are programmatically generated and successfully validated by the pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/07-thematic-dark-svgs/07-02-SUMMARY.md`
</output>
