---
phase: 08-algorithmic-background
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/algorithmic-art-zoltraak.md
  - README.template.md
autonomous: false
requirements:
  - ART-01
  - ART-02
must_haves:
  truths:
    - "A structured prompt is available to generate a Frieren-themed algorithmic background"
    - "The generated GIF artifact will be natively embedded in the dark-theme profile"
    - "The user can effortlessly export the looping GIF via p5.capture"
  artifacts:
    - path: "prompts/algorithmic-art-zoltraak.md"
      provides: "Instructions for Claude to generate the p5.js art with p5.capture"
      contains: "P5Capture.setDefaultOptions"
    - path: "README.template.md"
      provides: "Profile layout including the background embed"
      contains: "Zoltraak-dark-only.gif#gh-dark-mode-only"
  key_links:
    - from: "README.template.md"
      to: "assets/images/Zoltraak-dark-only.gif"
      via: "img tag src attribute"
      pattern: "Zoltraak-dark-only\\.gif#gh-dark-mode-only"
---

<objective>
Generate a structured prompt for the `algorithmic-art` skill to create a perfectly looping Frieren magic circle GIF, and prepare the profile template to display it.

Purpose: Bypasses complex headless recording by having the user generate and capture the loop interactively, ensuring a cohesive dark mode experience.
Output: Prompt file `prompts/algorithmic-art-zoltraak.md` and updated `README.template.md`.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-algorithmic-background/08-CONTEXT.md
@.planning/phases/08-algorithmic-background/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Algorithmic Art Prompt</name>
  <files>prompts/algorithmic-art-zoltraak.md</files>
  <action>
    Create the `prompts` directory if it does not exist. Then, write a detailed prompt to `prompts/algorithmic-art-zoltraak.md` targeting the `algorithmic-art` skill.
    The prompt MUST explicitly instruct the skill to:
    - Create a Frieren "Zoltraak Magic Circles" aesthetic (deep blues/purples, subtle rotating geometric circles with abstract runes, on a dark background).
    - Ensure rotational symmetry so it loops seamlessly over 80 frames.
    - Inject the `p5.capture` CDN script (`https://cdn.jsdelivr.net/npm/p5.capture@1.4.1/dist/p5.capture.umd.min.js`) into the HTML `<head>`.
    - Configure capture options inside `setup()`: `P5Capture.setDefaultOptions({ format: "gif", framerate: 20, duration: 80 })`.
    - Limit canvas size to a wide banner format (e.g., 800x200 or 800x300) to keep file size small.
  </action>
  <verify>
    <automated>node -e "require('assert')(require('fs').existsSync('prompts/algorithmic-art-zoltraak.md'))"</automated>
  </verify>
  <done>Prompt file is created with all required aesthetic and technical constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Embed GIF in README Template</name>
  <files>README.template.md</files>
  <action>
    Modify `README.template.md` to embed the anticipated GIF. Insert it right below the subtitle (`### Senior Frontend Engineer...`) inside a `<div align="center">` and `<picture>` tag.
    Use this exact string for the `<img>` tag to leverage the Phase 6 dark-mode-only logic: `<img src="assets/images/Zoltraak-dark-only.gif#gh-dark-mode-only" width="800" alt="Zoltraak Magic Circles Background">`.
    Run the build command `npm run build` (which will execute the template generator but won't fail since verification only targets SVGs).
  </action>
  <verify>
    <automated>node -e "const t = require('fs').readFileSync('README.template.md', 'utf8'); require('assert')(/Zoltraak-dark-only\\.gif#gh-dark-mode-only/.test(t))"</automated>
  </verify>
  <done>Template is updated with the embed logic and README.md is rebuilt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run Algorithmic Art Prompt</name>
  <action>Human step to execute the generated prompt and download the GIF.</action>
  <what-built>Prepared the prompt and template embed for the algorithmic background</what-built>
  <how-to-verify>
    1. In a new terminal window, run your AI agent and pass the contents of `prompts/algorithmic-art-zoltraak.md` to trigger the `algorithmic-art` skill.
    2. Once the HTML artifact is generated, open it in your browser.
    3. The `p5.capture` widget should automatically start recording and download a GIF named `capture.gif` (or similar).
    4. Rename the downloaded file to `Zoltraak-dark-only.gif` and move it to `assets/images/`.
    5. Run `npm run build` one last time and verify the GIF looks good in the profile's dark mode preview.
  </how-to-verify>
  <resume-signal>Type "approved" once the GIF is placed in assets/images/ and verified.</resume-signal>
</task>

</tasks>

<verification>
Overall phase check:
- `prompts/algorithmic-art-zoltraak.md` exists and contains correct instructions.
- `README.template.md` references the dark-mode-only GIF snippet correctly.
- Human verified the output GIF execution and generation.
</verification>

<success_criteria>
- Prompt correctly targets `algorithmic-art` and integrates `p5.capture`.
- Build pipeline runs without failing on missing GIF.
</success_criteria>

<output>
After completion, create `.planning/phases/08-algorithmic-background/08-01-SUMMARY.md`
</output>