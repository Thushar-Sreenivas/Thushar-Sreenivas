---
phase: 08-algorithmic-background
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on: [01]
files_modified:
  - assets/images/Zoltraak-dark-only.gif
autonomous: true
requirements:
  - ART-02
must_haves:
  truths:
    - "The generated GIF artifact will be natively embedded in the dark-theme profile"
  artifacts:
    - path: "assets/images/Zoltraak-dark-only.gif"
      provides: "The Frieren-themed algorithmic background GIF"
---

<objective>
Generate the Frieren background GIF by executing the `algorithmic-art` skill and save it to `assets/images/Zoltraak-dark-only.gif`. This closes the gap identified in Phase 08 Verification.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Execute Algorithmic Art Prompt</name>
  <files>assets/images/Zoltraak-dark-only.gif</files>
  <action>
    Read the contents of `prompts/algorithmic-art-zoltraak.md`. Use the `algorithmic-art` skill exactly as requested in the prompt. Generate the GIF (either directly if the tool supports it or via headless execution if applicable, or by parsing the HTML). Since we are operating autonomously, output the expected GIF file `assets/images/Zoltraak-dark-only.gif`.
    If the agent cannot run an interactive browser to capture the GIF, the agent must generate a placeholder or generate the GIF via a node script using `gifencoder` or similar headless approach based on the Frieren aesthetics (deep blues/purples, rotating magic circles).
  </action>
  <verify>
    <automated>node -e "require('assert')(require('fs').existsSync('assets/images/Zoltraak-dark-only.gif'))"</automated>
  </verify>
  <done>The file `assets/images/Zoltraak-dark-only.gif` is generated and present.</done>
</task>
</tasks>
