---
phase: 02-visual-design-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-svgs.js
  - assets/hero-light.svg
  - assets/hero-dark.svg
  - assets/footer-light.svg
  - assets/footer-dark.svg
autonomous: true
requirements:
  - VISL-01
must_haves:
  truths:
    - "Automated tests can verify SVG structures"
    - "User sees magical Frieren hero banner with quote at top of profile"
    - "User sees magical fading sparks footer at bottom of profile"
  artifacts:
    - path: "scripts/verify-svgs.js"
      provides: "Validation for responsive SVG requirements"
    - path: "assets/hero-light.svg"
      provides: "Light mode hero banner"
    - path: "assets/footer-dark.svg"
      provides: "Dark mode footer"
  key_links:
    - from: "scripts/verify-svgs.js"
      to: "assets/*.svg"
      via: "fs.readFileSync and regex"
      pattern: "viewBox"
---

<objective>
Build the validation script for SVG responsive requirements and create the primary Hero and Footer visual assets.

Purpose: Establishes the core "Frieren" visual identity and ensures SVGs are mobile-friendly (viewBox instead of fixed width/height).
Output: Test script and light/dark variants of Hero and Footer SVGs.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-design-system/02-CONTEXT.md
@.planning/phases/02-visual-design-system/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SVG verification script</name>
  <files>scripts/verify-svgs.js</files>
  <behavior>
    - Test 1: Given a directory of SVGs, verifies every SVG has a `viewBox` attribute and NO hardcoded `width` or `height` attributes on the root `<svg>` element.
    - Test 2: Verifies no `<image href="http...">` external resources exist to prevent GitHub Camo blocks.
    - Test 3: Ensures every `-light.svg` has a matching `-dark.svg` file.
  </behavior>
  <action>
    Create a Node.js script using native `assert` and `fs`/`path` modules that scans the `assets/` directory (excluding `images/`). Use regex to parse SVG tags. If validation fails, exit with code 1 and print errors. If no SVGs exist yet, pass.
  </action>
  <verify>
    <automated>node scripts/verify-svgs.js</automated>
  </verify>
  <done>Script exists, runs without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero Banner SVGs</name>
  <files>assets/hero-light.svg, assets/hero-dark.svg</files>
  <action>
    Create two SVGs for the Hero banner. 
    Use `viewBox="0 0 800 250"` and transparent background (`fill="none"`).
    Incorporate abstract magical elements (blue moon weed/Aurel flowers blowing in breeze).
    Embed the core quote text using `font-family="Georgia, serif"` and `letter-spacing`: "There's something about making code work that still feels like magic to me."
    Light mode colors: white/gold/soft-blue. Dark mode colors: deep purple/dark-blue/silver/teal.
    Add subtle CSS `@keyframes` animations (e.g. gentle floating or opacity pulsing).
  </action>
  <verify>
    <automated>node scripts/verify-svgs.js</automated>
  </verify>
  <done>Hero SVGs created and pass verification</done>
</task>

<task type="auto">
  <name>Task 3: Create Footer SVGs</name>
  <files>assets/footer-light.svg, assets/footer-dark.svg</files>
  <action>
    Create two SVGs for the profile footer.
    Use `viewBox="0 0 800 100"`.
    Depict a fading trail of magical sparks or petals.
    Follow the "Character Shift" theme (Frieren colors for light, Fern colors for dark).
    Must pass the responsive validation checks.
  </action>
  <verify>
    <automated>node scripts/verify-svgs.js</automated>
  </verify>
  <done>Footer SVGs created and pass verification</done>
</task>

</tasks>

<verification>
node scripts/verify-svgs.js passes for all created files
</verification>

<success_criteria>
- verify-svgs.js script exists and enforces mobile responsiveness for SVGs
- Hero and Footer SVGs created in light and dark mode variants
- All SVGs pass the verifier
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-design-system/02-01-SUMMARY.md`
</output>
