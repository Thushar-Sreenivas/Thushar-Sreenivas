---
phase: 02-visual-design-system
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - README.template.md
autonomous: false
requirements:
  - VISL-01
  - VISL-02
must_haves:
  truths:
    - "Generated README displays light/dark SVGs natively on GitHub"
    - "User can view the profile with magical assets correctly placed"
  artifacts:
    - path: "README.template.md"
      provides: "Markdown structure with image links"
  key_links:
    - from: "README.template.md"
      to: "assets/*.svg"
      via: "picture tags or hash fragments"
      pattern: "#gh-dark-mode-only"
---

<objective>
Integrate all custom Frieren SVGs into the README template and visually verify rendering.

Purpose: Wires up the visual design assets so the template engine can build the final README.
Output: Updated `README.template.md` and verified layout.
</objective>

<execution_context>
@/Users/thushar/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/thushar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-design-system/02-CONTEXT.md
@.planning/phases/02-visual-design-system/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject SVGs into README template</name>
  <files>README.template.md</files>
  <action>
    Replace text headers, standard dividers, and generic badges in `README.template.md` with the new SVGs.
    Use the GitHub fragment syntax for theme support:
    `![Hero](./assets/hero-light.svg#gh-light-mode-only)`
    `![Hero](./assets/hero-dark.svg#gh-dark-mode-only)`
    Do this for Hero, Dividers, Experience Header, Tech Header, and the three Bookshelves.
    Ensure alt text is descriptive.
  </action>
  <verify>
    <automated>grep -q "#gh-dark-mode-only" README.template.md</automated>
  </verify>
  <done>README.template.md contains proper GitHub theme image references</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual Verification Checkpoint</name>
  <action>Pause for user to visually verify the SVGs in the generated README.</action>
  <what-built>Visual Design System integrated into README.template.md</what-built>
  <how-to-verify>
    1. Run `node scripts/build-template.js` (if it exists) to generate the final `README.md`.
    2. Open `README.md` in a Markdown viewer (like VSCode preview or a GitHub gist preview).
    3. Toggle your theme between Light and Dark mode to ensure the correct SVGs display.
    4. Ensure the SVGs don't clip or look broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
grep checks for #gh-dark-mode-only syntax
</verification>

<success_criteria>
- README.template.md fully integrates the SVG assets
- User visually approves the aesthetics and layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-design-system/02-04-SUMMARY.md`
</output>
